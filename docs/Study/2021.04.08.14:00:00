- Tutorial:

    - The user tried to use with anaconda3 and received error.
    - The user downloaded python version 3.7.9 from the official web site and had a clean installation on a virtual environment.

- Study:

    - The user ask if he need to generate the code of the HeaderCommand because there were no references of code generation in the study order.
    - The user argue that the system create a text interface to python comands.